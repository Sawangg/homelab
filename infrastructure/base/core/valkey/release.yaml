---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: valkey
  namespace: valkey
spec:
  releaseName: valkey
  targetNamespace: valkey
  interval: 10m
  chart:
    spec:
      chart: valkey
      sourceRef:
        kind: OCIRepository
        name: bitnami
        namespace: valkey
  valuesFrom:
    - kind: ConfigMap
      name: valkey-values
