---
apiVersion: v1
kind: ConfigMap
metadata:
  name: renovate-cm
  namespace: renovate
data:
  # NOTE: Change the repositories name
  config.json: |-
    {
      "repositories": ["Sawangg/homelab"],
      "onboarding": false
    }
