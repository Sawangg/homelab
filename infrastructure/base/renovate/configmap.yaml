---
apiVersion: v1
kind: ConfigMap
metadata:
  name: renovate-cm
  namespace: renovate
data:
  # NOTE: Change the config to better match your use case
  config.json: |-
    {
      "platform": "github",
      "repositories": ["Sawangg/homelab"],
      "gitAuthor": "Renovate Bot <bot@renovateapp.com>",
      "onboarding": false
    }
