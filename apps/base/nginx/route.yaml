---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: nginx-route
  namespace: nginx
spec:
  parentRefs:
    - name: http-gateway
      namespace: default
  hostnames:
    - nginx.leomercier.dev
  rules:
    - backendRefs:
        - name: nginx
          port: 80
